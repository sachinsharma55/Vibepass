{
  "name": "vibepass",
  "version": "1.0.0",
  "description": "VibePass — event ticketing landing page (Bootstrap + nested SCSS)",
  "private": true,
  "scripts": {
    "sass:watch": "sass scss/main.scss:css/main.css --watch --source-map",
    "sass:build": "sass scss/main.scss:css/main.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.77.8"
  }
}
